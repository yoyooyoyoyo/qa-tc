# App v20 검진상세 쿠폰 노출 개선 TC

Source:

- Figma `착한의사APP`, node `87:11517`
- 기획서 섹션: `[쿠폰] 검진상세 노출 개선`

## 범위

- App 검진상세 화면의 쿠폰 카드 노출 개선을 검증한다.
- 주요 상태는 `다운로드 전`, `보유중`, `코드쿠폰 보유중`이다.
- AS-IS/TO-BE 비교 기준으로 기존 다운로드 후 쿠폰 카드가 흐리게 보이던 표현이 TO-BE 보유중 상태로 변경되는지 확인한다.
- 쿠폰 금액 예시는 Figma 기준 `정가 165,000원`, `쿠폰적용가 150,000원`이다.

## 테스트케이스

### TC-APP-COUPON-001

- title: 검진상세 진입 시 다운로드 가능한 쿠폰이 있으면 병원정보 영역에 쿠폰 카드가 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 로그인한 사용자
  - 검진상세 상품에 다운로드 가능한 쿠폰이 존재함
  - 사용자가 해당 쿠폰을 아직 보유하지 않음
- steps:
  1. App에서 쿠폰 대상 검진상품 상세 화면에 진입한다.
  2. 상단 병원정보와 병원공지 영역 하단을 확인한다.
- expected result:
  - 병원정보와 병원공지 하단에 쿠폰 카드가 노출된다.
  - 쿠폰 카드 하단에 검사정보 스크롤 탭바가 이어서 노출된다.
  - 쿠폰 카드와 스크롤 탭바가 겹치거나 가려지지 않는다.
- priority: High
- test_type: Functional
- notes: Figma TO-BE `다운로드 전` 상태 기준

### TC-APP-COUPON-002

- title: 다운로드 전 쿠폰 카드에 쿠폰명과 금액 정보가 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 다운로드 가능한 쿠폰이 존재함
  - 사용자가 해당 쿠폰을 아직 보유하지 않음
- steps:
  1. 쿠폰 대상 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드의 텍스트와 금액 영역을 확인한다.
- expected result:
  - 쿠폰명 영역이 노출된다.
  - 정가 `165,000원`이 쿠폰 카드의 기준 금액으로 노출된다.
  - `쿠폰적용가` 배지와 `150,000원` 금액이 함께 노출된다.
  - 쿠폰 설명 영역이 노출된다.
- priority: High
- test_type: Functional
- notes: 실제 금액은 테스트 데이터의 쿠폰 할인 정책에 따라 검증한다.

### TC-APP-COUPON-003

- title: 다운로드 전 쿠폰 카드에 `쿠폰받기` 버튼이 활성화 상태로 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 다운로드 가능한 쿠폰이 존재함 (발급 가능 상태)
  - 사용자가 해당 쿠폰을 아직 보유하지 않음
  - 코드쿠폰 미보유 or 다운로드쿠폰 금액이 더 큰 경우
- steps:
  1. 쿠폰 대상 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 영역의 버튼을 확인한다.
- expected result:
  - 쿠폰 카드에 `쿠폰받기` 버튼이 노출된다.
  - 버튼이 활성화(탭 가능) 상태로 노출된다.
  - `[최대 할인 적용중]` 뱃지는 노출되지 않는다.
- priority: High
- test_type: Functional
- notes: Figma 기획서 명시 버튼 문구: `쿠폰받기` (활성화 상태)

### TC-APP-COUPON-004

- title: 쿠폰 다운로드 시 `쿠폰받기` 버튼이 `[최대 할인 적용중]` 뱃지로 전환되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 다운로드 가능한 쿠폰이 존재함 (발급 가능 상태)
  - 사용자가 해당 쿠폰을 아직 보유하지 않음
- steps:
  1. 쿠폰 대상 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드의 `쿠폰받기` 버튼을 탭한다.
  3. 쿠폰 카드 영역을 다시 확인한다.
- expected result:
  - 쿠폰 다운로드가 완료된다.
  - `쿠폰받기` 버튼이 사라진다.
  - 쿠폰 적용가 우측에 `[최대 할인 적용중]` 뱃지가 노출된다.
  - 쿠폰 카드가 사라지지 않고 유지된다.
- priority: High
- test_type: State transition
- notes: Figma 기획서: 다운로드 후 쿠폰받기 버튼 사라지고 [최대 할인 적용중] 뱃지로 전환

### TC-APP-COUPON-005

- title: 보유중 쿠폰 카드에 `[최대 할인 적용중]` 뱃지가 노출되고 `쿠폰받기` 버튼은 없는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 대상 검진상품에 적용 가능한 다운로드쿠폰을 보유중임 (이미 다운로드 완료)
- steps:
  1. 쿠폰을 이미 보유한 사용자로 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드의 구성 요소와 뱃지 영역을 확인한다.
- expected result:
  - 쿠폰 카드가 AS-IS처럼 흐리거나 비활성으로 노출되지 않는다.
  - 쿠폰 적용가 우측에 `[최대 할인 적용중]` 뱃지가 노출된다.
  - `쿠폰받기` 버튼이 노출되지 않는다.
  - 쿠폰명, 정가(취소선), 쿠폰 적용가, 예약 가능 기간이 노출된다.
- priority: High
- test_type: Regression
- notes: Figma 기획서: 보유중 상태에는 쿠폰받기 버튼 없고 [최대 할인 적용중] 뱃지 노출

### TC-APP-COUPON-006

- title: 보유중 쿠폰 상태에서 하단 고정 CTA에 쿠폰적용가가 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 대상 검진상품에 적용 가능한 쿠폰을 보유중임
  - 상품 결제방식이 즉시결제임
- steps:
  1. 쿠폰 보유 사용자로 검진상품 상세 화면에 진입한다.
  2. 하단 고정 CTA 영역을 확인한다.
- expected result:
  - 하단 고정 CTA에 `쿠폰적용가` 배지가 노출된다.
  - 결제방식 라벨 `즉시결제`가 노출된다.
  - 쿠폰 적용 후 금액 `150,000원`이 노출된다.
  - `예약하기` 버튼이 노출된다.
- priority: High
- test_type: Functional
- notes: 금액은 Figma 예시 기준이며 실제 TC 데이터 금액으로 대체 가능

### TC-APP-COUPON-007

- title: 보유중 쿠폰 상태에서 찜 버튼과 예약하기 버튼이 함께 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 대상 검진상품에 적용 가능한 쿠폰을 보유중임
- steps:
  1. 쿠폰 보유 사용자로 검진상품 상세 화면에 진입한다.
  2. 하단 고정 CTA 영역의 좌측 아이콘 버튼과 우측 예약 버튼을 확인한다.
- expected result:
  - 하단 좌측에 사각 아이콘 버튼이 노출된다.
  - 하단 우측에 `예약하기` 버튼이 노출된다.
  - 두 버튼이 서로 겹치지 않고 각각 탭 가능한 영역을 가진다.
- priority: Medium
- test_type: UI
- notes: 좌측 아이콘의 실제 기능명은 구현/기획 기준 확인 필요

### TC-APP-COUPON-008

- title: 코드쿠폰 보유중 상태에서 쿠폰 카드가 보유중 UI로 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 대상 검진상품에 적용 가능한 코드쿠폰을 보유중임
- steps:
  1. 코드쿠폰 보유 사용자로 검진상품 상세 화면에 진입한다.
  2. 병원정보 하단 쿠폰 카드 영역을 확인한다.
- expected result:
  - 쿠폰 카드가 노출된다.
  - 쿠폰 카드에 쿠폰명, 정가, 쿠폰적용가, 쿠폰 설명이 노출된다.
  - 다운로드 전 상태가 아닌 보유중 상태의 UI로 노출된다.
- priority: High
- test_type: Functional
- notes: Figma TO-BE `코드쿠폰 보유중` 상태 기준

### TC-APP-COUPON-009

- title: 코드쿠폰 보유중 상태에서 하단 고정 CTA 금액이 쿠폰적용가로 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 대상 검진상품에 적용 가능한 코드쿠폰을 보유중임
  - 상품 결제방식이 즉시결제임
- steps:
  1. 코드쿠폰 보유 사용자로 검진상품 상세 화면에 진입한다.
  2. 하단 고정 CTA 영역을 확인한다.
- expected result:
  - 하단 고정 CTA에 `쿠폰적용가` 배지가 노출된다.
  - 결제 금액은 쿠폰 적용 후 금액으로 노출된다.
  - `예약하기` 버튼이 노출된다.
- priority: High
- test_type: Functional
- notes: 다운로드쿠폰 보유중 케이스와 동일한 가격 반영 정책인지 확인

### TC-APP-COUPON-010

- title: 쿠폰 카드 노출 시 기존 병원정보와 병원공지 정보가 유지되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 검진상품 상세 화면에 쿠폰 카드가 노출되는 상태
- steps:
  1. 검진상품 상세 화면에 진입한다.
  2. 병원정보 영역을 확인한다.
  3. 병원공지 영역을 확인한다.
  4. 쿠폰 카드 영역을 확인한다.
- expected result:
  - 병원명, 평점, 병원 규모 태그, 거리, 주소 정보가 노출된다.
  - 병원공지 영역이 노출된다.
  - 쿠폰 카드 추가로 기존 병원정보/공지 텍스트가 사라지거나 가려지지 않는다.
- priority: Medium
- test_type: Regression
- notes: Figma 병원명 예시는 `착한의사 인터맹크검진센터(스타필드 고양)`

### TC-APP-COUPON-011

- title: 쿠폰 카드 노출 시 검사정보 탭과 검사구성 영역이 정상적으로 이어지는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 검진상품 상세 화면에 쿠폰 카드가 노출되는 상태
- steps:
  1. 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 하단으로 스크롤한다.
  3. 스크롤 포인트 탭바와 검사정보 영역을 확인한다.
- expected result:
  - 스크롤 포인트 탭바가 쿠폰 카드 하단에 노출된다.
  - 검사정보 영역의 `검사구성`, `기본검사`, `선택검사`, `추가검사` 섹션이 정상 노출된다.
  - 쿠폰 카드 추가로 검사정보 영역 시작 위치가 겹치지 않는다.
- priority: Medium
- test_type: Regression
- notes: 화면 높이 변경에 따른 레이아웃 회귀 확인

### TC-APP-COUPON-012

- title: 쿠폰 적용 상품의 검사정보 영역에서 검사 그룹 정보가 유지되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 쿠폰 카드가 노출되는 검진상품 상세 화면
- steps:
  1. 검진상품 상세 화면에 진입한다.
  2. 검사정보 영역으로 이동한다.
  3. 검사구성 전체 영역의 검사 그룹을 확인한다.
- expected result:
  - `기본검사` 영역에 `기본으로 포함된 검사예요.` 설명이 노출된다.
  - `선택검사` 영역에 `그룹별로 선택할 수 있는 검사예요.` 설명이 노출된다.
  - `추가검사` 영역에 `추가할 수 있는 검사예요.` 설명이 노출된다.
  - 각 검사 그룹의 `미리보기` 액션이 노출된다.
- priority: Low
- test_type: Regression
- notes: 쿠폰 UI 변경이 검사정보 영역에 영향을 주지 않는지 확인

### TC-APP-COUPON-013

- title: 다운로드 전 상태에서 쿠폰 다운로드 후 화면 재진입 시 보유중 상태가 유지되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 다운로드 가능한 쿠폰이 존재함
  - 사용자가 해당 쿠폰을 아직 보유하지 않음
- steps:
  1. 검진상품 상세 화면에 진입한다.
  2. 쿠폰 다운로드 버튼을 탭한다.
  3. 검진상품 상세 화면을 이탈한다.
  4. 동일 검진상품 상세 화면에 다시 진입한다.
- expected result:
  - 재진입 후 쿠폰 카드가 다운로드 전 상태로 돌아가지 않는다.
  - 쿠폰 카드가 보유중 상태로 노출된다.
  - 하단 고정 CTA의 금액은 쿠폰 적용 후 금액으로 노출된다.
- priority: High
- test_type: State transition
- notes: 쿠폰 보유 상태 저장/API 반영 확인 필요

### TC-APP-COUPON-014

- title: 보유중 쿠폰 상태에서 예약하기 진입 시 쿠폰 적용 정보가 예약 플로우로 전달되는지 확인
- 테스트 진행 화면: App > 검진상세 > 예약하기
- precondition:
  - 사용자가 대상 검진상품에 적용 가능한 쿠폰을 보유중임
  - 하단 고정 CTA에 쿠폰적용가가 노출됨
- steps:
  1. 검진상품 상세 화면에 진입한다.
  2. 하단 `예약하기` 버튼을 탭한다.
  3. 예약/결제 화면의 할인혜택 또는 결제금액 영역을 확인한다.
- expected result:
  - 예약/결제 화면으로 이동한다.
  - 검진상세에서 노출된 쿠폰 적용 정보가 예약/결제 화면에도 반영된다.
  - 최종 결제금액이 쿠폰 적용 후 금액 기준으로 계산된다.
- priority: High
- test_type: Integration
- notes: 예약/결제 화면의 정확한 쿠폰 표기 위치는 별도 기획서 기준 확인

### TC-APP-COUPON-015

- title: 다운로드 가능한 쿠폰이 없는 상품에서는 쿠폰 카드가 노출되지 않는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 검진상품에 적용 가능한 다운로드쿠폰 또는 코드쿠폰이 없음
- steps:
  1. 쿠폰이 없는 검진상품 상세 화면에 진입한다.
  2. 병원공지 하단 영역을 확인한다.
  3. 하단 고정 CTA 가격 영역을 확인한다.
- expected result:
  - 병원공지 하단에 쿠폰 카드가 노출되지 않는다.
  - 스크롤 포인트 탭바가 쿠폰 카드 공백 없이 이어서 노출된다.
  - 하단 고정 CTA에 `쿠폰적용가` 배지가 노출되지 않는다.
- priority: Medium
- test_type: Negative
- notes: Figma 노드에 없는 보완 케이스이며 쿠폰 미대상 상품 데이터 필요

### TC-APP-COUPON-016

- title: 쿠폰 카드 금액과 하단 고정 CTA 금액이 동일한 쿠폰적용가로 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 대상 검진상품에 적용 가능한 쿠폰을 보유중임
  - 쿠폰 카드와 하단 고정 CTA가 모두 노출됨
- steps:
  1. 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드의 쿠폰적용가를 확인한다.
  3. 하단 고정 CTA의 쿠폰적용가를 확인한다.
- expected result:
  - 쿠폰 카드의 쿠폰적용가와 하단 고정 CTA의 쿠폰적용가가 동일하다.
  - Figma 예시 기준 두 영역 모두 `150,000원`으로 노출된다.
- priority: High
- test_type: Data consistency
- notes: 금액 포맷의 쉼표/원 단위 표기도 함께 확인

---

## 쿠폰 카드 노출 우선순위 로직 TC

> Source: Figma `기획서(2026)_착한의사App_v.20.0.0 ~`, node `1375:13921`
> 비교 대상: 운영중 다운로드쿠폰(발급가능) + 보유 다운로드쿠폰(사용기간내) + 보유 코드쿠폰 중 실제 할인 금액이 가장 큰 쿠폰을 노출한다.

### TC-APP-COUPON-027

- title: 다운로드 전 쿠폰 카드 구성 요소 (쿠폰명 / 정가 취소선 / 쿠폰적용가 / 예약 가능 기간 / 쿠폰받기 버튼)가 모두 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 다운로드 가능한 쿠폰이 존재함 (발급 가능 상태)
  - 사용자가 해당 쿠폰을 아직 보유하지 않음
- steps:
  1. 쿠폰 대상 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 전체 구성 요소를 확인한다.
- expected result:
  - 쿠폰명이 노출된다.
  - 정가가 취소선과 함께 노출된다 (예: ~~165,000원~~).
  - 쿠폰 적용가가 볼드체로 노출된다 (예: 150,000원).
  - 예약 가능 기간이 노출된다.
  - `쿠폰받기` 버튼이 활성화 상태로 노출된다.
- priority: High
- test_type: UI
- notes: Figma 기획서: 쿠폰명 / 정가(취소선) + 쿠폰 적용가 / 예약 가능 기간 / 쿠폰받기 버튼

### TC-APP-COUPON-028

- title: 보유중 쿠폰 카드 구성 요소 (쿠폰명 / 정가 취소선 / 쿠폰적용가 / 예약 가능 기간 / [최대 할인 적용중] 뱃지)가 모두 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 대상 검진상품에 적용 가능한 쿠폰을 보유중임 (다운로드쿠폰 또는 코드쿠폰)
- steps:
  1. 쿠폰 보유 사용자로 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 전체 구성 요소를 확인한다.
- expected result:
  - 쿠폰명이 노출된다.
  - 정가가 취소선과 함께 노출된다 (예: ~~165,000원~~).
  - 쿠폰 적용가가 볼드체로 노출된다 (예: 150,000원).
  - 쿠폰 적용가 우측에 `[최대 할인 적용중]` 뱃지가 노출된다.
  - 예약 가능 기간이 노출된다.
  - `쿠폰받기` 버튼이 노출되지 않는다.
- priority: High
- test_type: UI
- notes: Figma 기획서: 쿠폰명 / 정가(취소선) + 쿠폰 적용가 / 예약 가능 기간 / 최대 할인 적용 뱃지

### TC-APP-COUPON-029

- title: 서비스 이용료 할인 쿠폰 적용 시 적용가가 서비스 이용료 할인분만큼 차감되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 할인 영역이 `서비스 이용료(착한의사 케어비 + 할증료)`인 쿠폰이 존재함
  - 사용자가 해당 쿠폰을 보유 또는 발급받을 수 있는 상태
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드의 정가와 쿠폰 적용가를 확인한다.
  3. 서비스 이용료 할인분 = 정가 - 쿠폰 적용가 인지 검증한다.
- expected result:
  - 정가에서 서비스 이용료(케어비 + 할증료) 할인분만큼 차감된 금액이 쿠폰 적용가로 표시된다.
  - 정가 전체에서 쿠폰 금액이 차감되는 방식이 아님을 확인한다.
- priority: High
- test_type: Functional
- notes: Figma 기획서: 할인 영역 — 서비스 이용료 → 정가에서 서비스 이용료 할인분만큼 차감. 할인 영역 구분 필요.

### TC-APP-COUPON-030

- title: 전체할인 쿠폰 적용 시 적용가가 정가에서 쿠폰 금액 전체 차감되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 할인 영역이 `전체할인`인 쿠폰이 존재함
  - 사용자가 해당 쿠폰을 보유 또는 발급받을 수 있는 상태
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드의 정가와 쿠폰 적용가를 확인한다.
  3. 쿠폰 적용가 = 정가 - 쿠폰 금액 인지 검증한다.
- expected result:
  - 정가에서 쿠폰 금액 전체를 차감한 금액이 쿠폰 적용가로 표시된다.
  - 서비스 이용료 할인 방식과 다른 계산 결과임을 확인한다.
- priority: High
- test_type: Functional
- notes: Figma 기획서: 할인 영역 — 전체할인 → 정가에서 쿠폰 금액 전체 차감.

### TC-APP-COUPON-031

- title: 쿠폰 미보유 시 검진상세 하단 고정 바에 쿠폰 라벨이 노출되지 않는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 해당 검진상품에 적용 가능한 쿠폰을 미보유 상태임
  - 다운로드 가능한 쿠폰이 운영 중인 상태 (쿠폰 카드는 노출됨)
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 하단 고정 바(가격 + 예약하기 버튼 영역)를 확인한다.
- expected result:
  - 하단 고정 바에 `쿠폰적용가` 라벨이 노출되지 않는다.
  - 정가만 노출된다.
  - `예약하기` 버튼은 정상 노출된다.
- priority: Medium
- test_type: Functional
- notes: Figma 기획서: 보유중인 쿠폰이 1개 이상인 경우에만 하단 바 쿠폰 라벨 노출.

### TC-APP-COUPON-032

- title: 쿠폰 영역 전체 hide 상태에서 하단 고정 바에도 정가만 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 다운로드 쿠폰 미운영 + 코드쿠폰 미보유 (쿠폰 영역 hide 조건 충족)
- steps:
  1. 쿠폰 영역이 hide 처리된 검진상품 상세 화면에 진입한다.
  2. 하단 고정 바(가격 + 예약하기 버튼 영역)를 확인한다.
- expected result:
  - 쿠폰 카드 영역이 노출되지 않는다.
  - 하단 고정 바에 `쿠폰적용가` 라벨이 노출되지 않는다.
  - 하단 고정 바에 정가만 노출된다.
- priority: High
- test_type: Functional
- notes: Figma 기획서: 쿠폰 영역 hide 상태(미운영·미보유)에서 하단 바도 정가만 노출.

### TC-APP-COUPON-033

- title: 다운로드 가능 쿠폰 카드는 코드쿠폰 금액이 더 큰 경우 노출되지 않는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 운영 중인 다운로드쿠폰이 존재함 (예: 30,000원)
  - 사용자가 코드쿠폰을 보유중임 (예: 50,000원)
  - 코드쿠폰 금액 > 다운로드쿠폰 금액
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 영역을 확인한다.
- expected result:
  - 다운로드 가능 상태의 쿠폰 카드(쿠폰받기 버튼 포함)가 노출되지 않는다.
  - 코드쿠폰 기준의 보유중 쿠폰 카드가 노출된다 (`[최대 할인 적용중]` 뱃지 포함).
- priority: High
- test_type: Functional
- notes: Figma 기획서: 코드쿠폰 미보유 or 다운로드쿠폰 금액이 더 큰 경우에만 다운로드 가능 카드 노출.

---

## 쿠폰 카드 노출 우선순위 로직 TC

### TC-APP-COUPON-017

- title: 다운로드쿠폰 금액이 코드쿠폰보다 클 때 다운로드쿠폰이 노출되는지 확인 (케이스 ①)
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 코드쿠폰을 보유중임 (예: 할인금액 30,000원)
  - 동일 검진상품에 운영 중인 다운로드쿠폰이 존재함 (예: 할인금액 50,000원)
  - 다운로드쿠폰 금액 > 코드쿠폰 금액
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 영역을 확인한다.
- expected result:
  - 쿠폰 카드에 금액이 더 큰 다운로드쿠폰이 노출된다.
  - 코드쿠폰이 아닌 다운로드쿠폰 기준의 할인금액과 쿠폰명이 표시된다.
  - 코드쿠폰 정보는 쿠폰 카드에 노출되지 않는다.
- priority: High
- test_type: Functional
- notes: 기획서 케이스 ① 기준. 다운로드쿠폰 미보유/보유 모두 해당.

### TC-APP-COUPON-018

- title: 코드쿠폰 금액이 다운로드쿠폰보다 클 때 코드쿠폰이 노출되는지 확인 (케이스 ②)
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 코드쿠폰을 보유중임 (예: 할인금액 80,000원)
  - 동일 검진상품에 운영 중인 다운로드쿠폰이 존재함 (예: 할인금액 50,000원)
  - 코드쿠폰 금액 > 다운로드쿠폰 금액
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 영역을 확인한다.
- expected result:
  - 쿠폰 카드에 금액이 더 큰 코드쿠폰이 노출된다.
  - 다운로드쿠폰이 아닌 코드쿠폰 기준의 할인금액과 쿠폰명이 표시된다.
  - 다운로드쿠폰 정보는 쿠폰 카드에 노출되지 않는다.
- priority: High
- test_type: Functional
- notes: 기획서 케이스 ② 기준.

### TC-APP-COUPON-019

- title: 다운로드쿠폰과 코드쿠폰 금액이 동일하고 종료일이 다를 때 종료일이 빠른 쿠폰이 노출되는지 확인 (케이스 ③-a)
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 코드쿠폰을 보유중임 (예: 할인금액 50,000원, 사용 종료일 2026-06-30)
  - 동일 검진상품에 다운로드쿠폰이 존재함 (예: 할인금액 50,000원, 사용 종료일 2026-05-31)
  - 두 쿠폰의 할인금액이 동일하고, 다운로드쿠폰의 종료일이 더 빠름
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 영역을 확인한다.
- expected result:
  - 쿠폰 카드에 사용 종료일이 더 빠른 다운로드쿠폰이 노출된다.
  - 코드쿠폰이 아닌 다운로드쿠폰 정보가 표시된다.
- priority: High
- test_type: Functional
- notes: 기획서 케이스 ③ 기준. 종료일 역방향(코드쿠폰이 빠른 경우)도 검증 필요.

### TC-APP-COUPON-020

- title: 다운로드쿠폰과 코드쿠폰 금액과 종료일이 모두 동일할 때 코드쿠폰이 우선 노출되는지 확인 (케이스 ③-b)
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 코드쿠폰을 보유중임 (예: 할인금액 50,000원, 사용 종료일 2026-05-31)
  - 동일 검진상품에 다운로드쿠폰이 존재함 (예: 할인금액 50,000원, 사용 종료일 2026-05-31)
  - 두 쿠폰의 할인금액과 사용 종료일이 모두 동일함
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 영역을 확인한다.
- expected result:
  - 쿠폰 카드에 코드쿠폰이 노출된다.
  - 다운로드쿠폰이 아닌 코드쿠폰 정보(쿠폰명, 금액)가 표시된다.
- priority: High
- test_type: Functional
- notes: 기획서 정책: 코드쿠폰은 유저가 직접 입력한 쿠폰이므로 우선 적용.

### TC-APP-COUPON-021

- title: 다운로드쿠폰이 미운영 상태이고 코드쿠폰도 미보유 시 쿠폰 영역이 노출되지 않는지 확인 (케이스 ④ / hide 조건 1)
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 해당 검진상품에 다운로드쿠폰이 미운영 상태임 (Admin에서 쿠폰 중지 또는 등록되지 않음)
  - 사용자가 코드쿠폰을 미보유 상태임
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 병원공지 하단 영역을 확인한다.
  3. 하단 고정 CTA 영역을 확인한다.
- expected result:
  - 쿠폰 카드 영역이 전체 노출되지 않는다 (hide 처리).
  - 병원공지 하단에 쿠폰 카드 공백이 없이 스크롤 탭바가 바로 이어진다.
  - 하단 CTA에 `쿠폰적용가` 배지가 노출되지 않는다.
- priority: High
- test_type: Negative
- notes: 기획서 hide 조건 ①. Admin 쿠폰 상태를 '중지' 또는 미등록으로 세팅하여 재현.

### TC-APP-COUPON-022

- title: 다운로드쿠폰의 1인당 발급 수량이 소진되고 코드쿠폰도 미보유 시 쿠폰 영역이 노출되지 않는지 확인 (hide 조건 2)
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 해당 검진상품에 다운로드쿠폰이 운영 중이나 해당 사용자의 1인당 발급 수량이 이미 소진됨
  - 사용자가 코드쿠폰을 미보유 상태임
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 병원공지 하단 영역을 확인한다.
  3. 하단 고정 CTA 영역을 확인한다.
- expected result:
  - 쿠폰 카드 영역이 전체 노출되지 않는다 (hide 처리).
  - 하단 CTA에 `쿠폰적용가` 배지가 노출되지 않는다.
- priority: High
- test_type: Negative
- notes: 기획서 hide 조건 ②. 1인당 수량 소진은 Admin에서 해당 사용자 기준으로 재현 필요.

### TC-APP-COUPON-023

- title: 다운로드쿠폰의 발급 기간이 만료되고 코드쿠폰도 미보유 시 쿠폰 영역이 노출되지 않는지 확인 (hide 조건 3)
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 해당 검진상품의 다운로드쿠폰 발급 기간이 이미 만료됨 (Admin 쿠폰 상태: 종료)
  - 사용자가 코드쿠폰을 미보유 상태임
  - (단, 기존 보유 쿠폰이 있다면 사용기간 내 여부 별도 확인)
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 병원공지 하단 영역을 확인한다.
  3. 하단 고정 CTA 영역을 확인한다.
- expected result:
  - 쿠폰 카드 영역이 전체 노출되지 않는다 (hide 처리).
  - 하단 CTA에 `쿠폰적용가` 배지가 노출되지 않는다.
- priority: High
- test_type: Negative
- notes: 기획서 hide 조건 ③. 발급 기간 만료 + 미보유 조합임. 발급 기간 만료 + 기존 보유 쿠폰 사용기간 내 케이스는 정책 노트 별도 확인.

### TC-APP-COUPON-024

- title: 다운로드쿠폰이 미운영이더라도 코드쿠폰을 보유 중이면 쿠폰 카드가 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 해당 검진상품에 다운로드쿠폰이 미운영 상태임
  - 사용자가 코드쿠폰을 보유중임
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 병원공지 하단 영역을 확인한다.
- expected result:
  - 쿠폰 카드 영역이 노출된다.
  - 코드쿠폰 기준의 쿠폰 카드가 노출된다.
  - 쿠폰 카드가 hide 처리되지 않는다.
- priority: High
- test_type: Functional
- notes: hide 조건은 "다운로드 쿠폰 미운영 + 코드쿠폰 미보유"일 때만 적용. 코드쿠폰 보유 시에는 hide 아님.

### TC-APP-COUPON-025

- title: 보유 중인 다운로드쿠폰이 여러 개일 때 할인금액이 가장 큰 쿠폰이 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 동일 검진상품에 적용 가능한 다운로드쿠폰 여러 개를 보유중임 (예: A 50,000원, B 30,000원, C 70,000원)
  - 코드쿠폰 미보유
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 영역을 확인한다.
- expected result:
  - 쿠폰 카드에 할인금액이 가장 큰 쿠폰(예: C 70,000원)이 노출된다.
  - 금액이 작은 쿠폰(A, B)은 카드에 노출되지 않는다.
- priority: High
- test_type: Functional
- notes: 비교 풀 정책 기준. 운영 중 다운로드쿠폰(발급가능) + 보유 다운로드쿠폰(사용기간내) 모두 포함하여 비교.

### TC-APP-COUPON-026

- title: 운영 중 다운로드쿠폰(미보유)과 보유 중 다운로드쿠폰 금액 비교 시 큰 금액이 노출되는지 확인
- 테스트 진행 화면: App > 검진상세
- precondition:
  - 사용자가 다운로드쿠폰 A를 보유중임 (예: 50,000원, 사용기간 내)
  - 동일 검진상품에 운영 중인 다운로드쿠폰 B가 별도로 존재함 (예: 70,000원, 발급 가능)
  - 코드쿠폰 미보유
- steps:
  1. 해당 검진상품 상세 화면에 진입한다.
  2. 쿠폰 카드 영역을 확인한다.
- expected result:
  - 쿠폰 카드에 할인금액이 더 큰 운영중 다운로드쿠폰 B(70,000원)가 노출된다.
  - 보유 중인 A 쿠폰이 아닌 B 쿠폰 정보가 표시된다.
- priority: High
- test_type: Functional
- notes: 비교 대상은 미보유 운영쿠폰도 포함. 발급 가능 상태여야 함.

---

## 확인 필요

- 다운로드 버튼의 정확한 문구와 다운로드 완료 후 버튼 문구는 Figma 컴포넌트 원문 또는 실제 구현 기준으로 확인한다.
- 쿠폰 다운로드 성공/실패 토스트 문구와 실패 케이스는 별도 기획서 또는 API 정책 기준으로 보강한다.
- 코드쿠폰 보유중 상태가 다운로드쿠폰 보유중 상태와 동일한 가격 반영 정책인지 API 응답 기준으로 확인한다.
- 현장결제 상품에서 쿠폰 카드와 하단 가격이 어떻게 표기되는지는 별도 데이터로 확인한다.
- 발급 기간 만료 + 기존 보유 쿠폰 사용기간 내 케이스에서 쿠폰 카드 노출 여부를 별도 정책 기준으로 확인한다.
